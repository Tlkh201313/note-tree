<!-- Thanks for contributing. Keep this short — a sentence per line is plenty. -->

## What this changes

## Why

## Checklist

- [ ] `node test/run.mjs` passes
- [ ] `node test/bench.mjs` passes (SessionStart stays under 150 ms at 1,000 notes)
- [ ] No new runtime dependencies (`dependencies` is still empty)
- [ ] New behaviour has an assertion
- [ ] If this touches a hook: it still exits `0` and prints nothing when it fails

<!-- Adding an agent adapter? Please link the first-party docs for the paths you
used, or mark the entry confidence: 'community'. -->
